specVersion: "v1beta"
serviceId: "frontend-repo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "frontend-repo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
